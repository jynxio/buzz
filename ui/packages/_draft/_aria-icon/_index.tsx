// import clsx from 'clsx';
// import css from './_index.module.css';

// function $<P extends { className?: string }>(props: { as: React.ComponentType<P>; props: P }) {
//     const { as: Comp, props: compProps } = props;
//     const className = clsx(compProps.className, css['root']);

//     return <Comp {...compProps} className={className} />;
// }

// export { $ as AriaIcon };
